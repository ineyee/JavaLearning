<?xml version="1.0" encoding="UTF-8"?>

<database-config>
    <!-- MySQL 数据库驱动库的类名 -->
    <driverClassName>com.mysql.cj.jdbc.Driver</driverClassName>

    <!--
        jdbc:mysql://：MySQL 的固定写法
        localhost、3306：域名和端口号
        db_hello_mysql：数据库的名称
        serverTimezone=UTC：告诉数据库驱动 MySQL 已经把默认时区设置为了 0 时区
        root、mysqlroot：数据库的用户名和密码
    -->
    <url>jdbc:mysql://localhost:3306/db_hello_mysql?serverTimezone=UTC</url>
    <username>root</username>
    <password>mysqlroot</password>
</database-config>