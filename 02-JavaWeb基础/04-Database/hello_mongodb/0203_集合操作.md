一个数据库里可以有多个集合（即多张表）

## 一、创建集合

> 注意：MongoDB 里的集合没有固定的数据结构、也就是说没有固定的字段，所以创建集合非常简单，不需要像创建表那样提前指定表的所有字段。集合创建出来后空着放那就行了，后面往里放什么数据都行，集合里每一条数据的结构可以不同

```javascript
db.createCollection("product");
```

## 二、删除集合

```SQL
db.product.drop();
```
