<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    namespace：当前文件的命名空间，可以理解为当前文件里所有 SQL 语句的默认前缀，避免当前文件里 SQL 语句的唯一标识和别的文件里 SQL 语句的唯一标识重复
    一般命名为：公司域名倒写 + dao + 某个模块具体的 Dao（因为这里就相当于是某个模块的数据层了）
-->
<mapper namespace="com.ineyee.dao.UserDao">
    <!--
        select 标签里用来写“查”的 SQL 语句
            id：这条 SQL 语句的唯一标识，可以按照 dao 层方法的命名规范来命名
            resultType：从数据库表里查询出来一条一条的数据，会自动转换成 UserBean 类型的对象
    -->
    <select id="list" resultType="bean.UserBean">
        SELECT *
        FROM t_user;
    </select>
</mapper>