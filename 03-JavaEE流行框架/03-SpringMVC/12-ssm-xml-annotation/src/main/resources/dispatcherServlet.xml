<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!--
        通过 context:component-scan 标签告诉 SpringMVC 框架哪个包里的 Controller 类是通过注解实现 IoC 的
        SpringMVC 框架就会扫描这个包里所有有注解的类来自动创建对象并放到 IoC 子容器里

        子容器需要扫描：
        1. controller 所在的包
        2. 全局异常处理器所在的包（@ControllerAdvice）
    -->
    <context:component-scan base-package="com.ineyee.controller"/>
    <context:component-scan base-package="com.ineyee.api.exception"/>

    <!--
        DispatcherServlet 虽然拦截到了静态资源
        但是我们不让它处理，而是转交给默认的静态资源 Servlet 走服务器默认的处理
    -->
    <mvc:default-servlet-handler/>
    <!--
        用途一：
            但是添加了 mvc:default-servlet-handler 后又会导致 @Controller 等注解无法处理接口型请求
            所以还得加上 mvc:annotation-driven 注解驱动来保证 @Controller 等注解可以正常处理接口型请求
        用途二：
            自动启用参数校验功能（@Valid 等）
        用途三：
            自动注册消息转换器（mvc:message-converters）
    -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <!-- 配置一下当响应体为 String 时的编码方式 -->
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="defaultCharset" value="utf-8"/>
            </bean>
            <!-- 配置一下当响应体为 Java 对象或 Map 时的编码方式 -->
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="defaultCharset" value="utf-8"/>

                <!--
                    默认情况下 Jackson 的 JavaTimeModule 默认会将 LocalDateTime 序列化为数组格式 [年、月、日、时、分、秒]
                    需要禁用 WRITE_DATES_AS_TIMESTAMPS 特性，这样 LocalDateTime 就会被序列化为 ISO-8601 字符串格式，而不是数组格式
                -->
                <property name="objectMapper">
                    <bean class="org.springframework.http.converter.json.Jackson2ObjectMapperFactoryBean">
                        <property name="featuresToDisable">
                            <array>
                                <bean id="WRITE_DATES_AS_TIMESTAMPS"
                                      class="org.springframework.beans.factory.config.FieldRetrievingFactoryBean">
                                    <property name="staticField"
                                              value="com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS"/>
                                </bean>
                            </array>
                        </property>
                    </bean>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 配置验证器，用于校验参数是否必传 -->
    <bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean"/>
    <!-- 启用方法级别的验证 -->
    <bean class="org.springframework.validation.beanvalidation.MethodValidationPostProcessor"/>

    <!--
        在 Spring 的配置文件里把 commons-fileupload 三方库的 StandardServletMultipartResolver 添加到 IoC 容器，id 值固定为 multipartResolver
        将来 SpringMVC 就会通过这个 id 值拿到这个解析器去解析 multipart/form-data 请求的参数
    -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

    <!-- 添加一下拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--
                设置拦截器能拦截那些请求：这里的请求就是我们通过 / 设置了 DispatcherServlet 会拦截接口型请求 + 会拦截静态资源型请求这两种
                虽然 DispatcherServlet 把静态资源型请求转交给服务器默认的处理了，但拦截它肯定是会先拦截下来的，也就是说先拦截后转交，而不是直接转交

                /*：代表 http://xxx/applicationContext/xxx 这种一级路径的请求才会被拦截
                /**：代表 http://xxx/applicationContext/xxx、http://xxx/applicationContext/xxx/xxx 这种一级路径或 N 级路径的请求都会被拦截
            -->
            <mvc:mapping path="/**"/>
            <!--
                如果我们把静态资源都放在 applicationContext/asset 目录下，这里就代表不要拦截 asset 及其子目录下的静态资源请求
                这样一来拦截器就是只拦截接口型请求了
            -->
            <mvc:exclude-mapping path="/asset/**"/>
            <!-- 我们自定义的 HTTP 请求日志拦截器 -->
            <bean class="com.ineyee.interceptor.HttpLogInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>